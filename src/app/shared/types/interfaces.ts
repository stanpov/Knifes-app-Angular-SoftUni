export interface UserData {
    firstName: string;
    lastName: string;
    avatar: string;
    email: string;
    id?: string;
  }

export interface carouselImages {
  imageSrc: string,
  altTitle: string,
  id: string
}